# chrome-auto-refresh
A lightweight Chrome extension that refreshes one or more selected tabs on a configurable timer, with optional hard reloads, active-tab rotation, live countdown, refresh counter, and persistent settings.
